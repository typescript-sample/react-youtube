import { SearchComponentState } from "react-hook-core";
import { User, UserFilter } from "./user";

interface UserSearch extends SearchComponentState<User, UserFilter> {
   
  }

export const UserSearchPage = () => {
  return(
    <div></div>
  )
}